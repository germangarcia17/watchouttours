/* Dominio canónico de producción. Se usa para construir canonical y
   hreflang siempre con el mismo host, aunque el sitio se sirva también
   desde un dominio de vista previa (Netlify) o un alias www. */
export const SITE_URL = 'https://watchouttours.nz'
